// ══════════════════════════════════════════════════════════════
//  ★ 模板 —— 這個檔案永遠不要改
//
//  用法:Ctrl+A 全選 -> Ctrl+C -> 到 solve.cpp 全選貼上,蓋掉舊的
// ══════════════════════════════════════════════════════════════

// ══════════════════════════════════════════════════════════════
//  ★ 這一題(AC 之後,連 code 一起存進 solved/<rating>/)
//  CF_____  題目名            rating _____
//  https://codeforces.com/problemset/problem/_____/_
//  用時 ___min
// ──────────────────────────────────────────────────────────────
//  題目一句話 :
//  卡在哪     :
//  關鍵想法   :
//  線索       : ★★ 下次看到「        」就要想到「        」
// ══════════════════════════════════════════════════════════════

#include <bits/stdc++.h>
using namespace std;

using ll  = long long;
using ull = unsigned long long;
using pii = pair<int,int>;
using pll = pair<ll,ll>;
using vi  = vector<int>;
using vll = vector<ll>;

#define pb      push_back
#define all(x)  (x).begin(), (x).end()
#define rall(x) (x).rbegin(), (x).rend()
#define sz(x)   (int)(x).size()
#define fi first
#define se second

const ll  INF = LLONG_MAX / 4;
const int MOD = 1000000007;

// ── debug:交題之前不用刪。它只在 Debug 組態會印,Release 自動消失 ──
#ifdef LOCAL
    #define dbg(x) cerr << #x << " = " << (x) << endl
#else
    #define dbg(x)
#endif



void solve() {




}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    cin >> t;              // ← 單筆測資的題目,把這行註解掉
    while (t--) solve();
    return 0;
}
